---
import pdfLogo from '../assets/logo.svg';
---
<div id="container">
    <nav>
        <div class="logo">
            <img src={pdfLogo.src} alt="PDFtoText Logo" class="nav-logo">
            <span>PDFtoText</span>
        </div>
    </nav>

    <main>
        <h1>
            <img src={pdfLogo.src} alt="PDFtoText Logo" class="main-logo">
            PDFtoText
        </h1>
        <p>Convert PDF to TXT is a simple and fast macOS app that transforms your PDF files into editable text. Perfect for easily extracting content and working with it in any text editor.</p>
        <video autoplay muted loop width="640" height="360">
            <source src="/pdftext.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
        <a href="#" class="download-button">Download</a>
    </main>

    <footer>
        <a href="https://github.com/fedebarco/PDFtoText/blob/main/LICENSE">License</a>
        <a href="https://github.com/fedebarco/PDFtoText/tree/main">Source Code</a>
        <a href="/privacy">Privacy Policy</a>
    </footer>
</div>

<style>
:root {
    --init-background-color: rgb(28, 28, 30);
    --init-label-color: rgba(255, 255, 255, 0.98);
    --secondary-color: rgba(255, 255, 255, 0.6);
}

body {
    background-color: var(--init-background-color);
    color: var(--init-label-color);
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Helvetica Neue", sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
    letter-spacing: -0.022em;
}

nav {
    background-color: rgba(28, 28, 30, 0.8);
    padding: 16px 40px;
    width: 100%;
    text-align: left;
    box-sizing: border-box;
    backdrop-filter: blur(20px);
    position: fixed;
    top: 0;
    z-index: 100;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.logo {
    font-size: 1.2em;
    font-weight: 600;
    color: var(--init-label-color);
    display: flex;
    align-items: center;
    gap: 12px;
}

.logo span {
    color: white;
}

.nav-logo {
    width: 28px;
    height: 28px;
    object-fit: contain;
    filter: brightness(0) invert(1);
}

main {
    text-align: center;
    padding: 120px 20px 40px;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    max-width: 980px;
    margin: 0 auto;
}

main h1 {
    font-size: 56px;
    margin: 0 0 20px;
    font-weight: 500;
    letter-spacing: -0.05em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
}

.main-logo {
    width: 72px;
    height: 72px;
    object-fit: contain;
    filter: brightness(0) invert(1);
}

main p {
    font-size: 21px;
    margin-bottom: 40px;
    color: var(--init-label-color);
    line-height: 1.47059;
    max-width: 640px;
    margin-left: auto;
    margin-right: auto;
    text-align: center;
}

video {
    margin-bottom: 40px;
    border-radius: 18px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    max-width: 100%;
    height: auto;
}

.download-button {
    background-color: var(--init-label-color);
    color: var(--init-background-color);
    padding: 12px 24px;
    text-decoration: none;
    border-radius: 980px;
    font-size: 17px;
    font-weight: 400;
    display: inline-block;
    transition: all 0.2s ease;
}

.download-button:hover {
    transform: scale(1.02);
    background-color: #fff;
}

footer {
    width: 100%;
    text-align: center;
    padding: 20px;
    box-sizing: border-box;
}

footer a {
    color: var(--secondary-color);
    text-decoration: none;
    margin: 0 10px;
    font-size: 12px;
    transition: color 0.2s ease;
}

footer a:hover {
    color: var(--init-label-color);
}
</style>
