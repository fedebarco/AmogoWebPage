---
import pdfLogo from '../assets/logo.svg';
---
<div id="container">
    <nav>
        <div class="logo">
            <img src={pdfLogo.src} alt="PDFtoText Logo" class="nav-logo">
            <span>PDFtoText</span>
        </div>
    </nav>

    <main class="privacy-content">
        <h1>Privacy Policy</h1>
        
        <section>
            <h2>No Data Collection</h2>
            <p>
                PDFtoText is designed with your privacy in mind. We want to be clear about our stance on data collection:
                we don't collect any personal information or data from our users.
            </p>
        </section>

        <section>
            <h2>How PDFtoText Works</h2>
            <p>
                PDFtoText operates entirely on your local machine. When you convert a PDF file:
            </p>
            <ul>
                <li>All processing happens locally on your computer</li>
                <li>No files are uploaded to any servers</li>
                <li>No usage data is collected or transmitted</li>
                <li>No analytics or tracking tools are implemented</li>
            </ul>
        </section>

        <section>
            <h2>File Access</h2>
            <p>
                The app only accesses the PDF files you explicitly choose to convert. These files remain on your computer
                and are never transmitted elsewhere.
            </p>
        </section>

        <section>
            <h2>Contact</h2>
            <p>
                If you have any questions about our privacy practices, feel free to reach out at 
                <a href="mailto:fbarcoo@unal.edu.co" class="inline-link">fbarcoo@unal.edu.co</a>.
            </p>
        </section>
    </main>

    <footer>
        <a href="https://github.com/fedebarco/PDFtoText/blob/main/LICENSE">License</a>
        <a href="https://github.com/fedebarco/PDFtoText/tree/main">Source Code</a>
        <a href="/">Home</a>
    </footer>
</div>

<style>
:root {
    --init-background-color: rgb(28, 28, 30);
    --init-label-color: rgba(255, 255, 255, 0.98);
    --secondary-color: rgba(255, 255, 255, 0.6);
}

body {
    background-color: var(--init-background-color);
    color: var(--init-label-color);
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Helvetica Neue", sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
    letter-spacing: -0.022em;
}

nav {
    background-color: rgba(28, 28, 30, 0.8);
    padding: 16px 40px;
    width: 100%;
    text-align: left;
    box-sizing: border-box;
    backdrop-filter: blur(20px);
    position: fixed;
    top: 0;
    z-index: 100;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.logo {
    font-size: 1.2em;
    font-weight: 600;
    color: var(--init-label-color);
    display: flex;
    align-items: center;
    gap: 12px;
}

.logo span {
    color: white;
}

.nav-logo {
    width: 28px;
    height: 28px;
    object-fit: contain;
    filter: brightness(0) invert(1);
}

main {
    text-align: center;
    padding: 120px 20px 40px;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    max-width: 800px;
    margin: 0 auto;
}

.privacy-content {
    text-align: left;
}

.privacy-content h1 {
    font-size: 48px;
    margin: 0 0 40px;
    font-weight: 500;
    letter-spacing: -0.05em;
    text-align: center;
}

.privacy-content section {
    margin-bottom: 40px;
}

.privacy-content h2 {
    font-size: 24px;
    margin: 0 0 16px;
    font-weight: 500;
    color: var(--init-label-color);
}

.privacy-content p {
    font-size: 17px;
    line-height: 1.5;
    color: var(--secondary-color);
    margin: 0 0 16px;
}

.privacy-content ul {
    list-style-type: none;
    padding: 0;
    margin: 16px 0;
}

.privacy-content li {
    font-size: 17px;
    line-height: 1.5;
    color: var(--secondary-color);
    margin: 8px 0;
    padding-left: 24px;
    position: relative;
}

.privacy-content li:before {
    content: "•";
    position: absolute;
    left: 0;
    color: var(--init-label-color);
}

.inline-link {
    color: var(--init-label-color);
    text-decoration: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.3);
    transition: border-color 0.2s ease;
}

.inline-link:hover {
    border-color: var(--init-label-color);
}

footer {
    width: 100%;
    text-align: center;
    padding: 20px;
    box-sizing: border-box;
}

footer a {
    color: var(--secondary-color);
    text-decoration: none;
    margin: 0 10px;
    font-size: 12px;
    transition: color 0.2s ease;
}

footer a:hover {
    color: var(--init-label-color);
}
</style> 